Tell Express what to do for each URL.

Middleware

Functions executed before routes (app.use()).

Router

Splits routes into separate files (clean code).

MVC

Model → data

View → front-end templates (optional in API)

Controller → logic

Route → URL mapping to controllers